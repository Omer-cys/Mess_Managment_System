Run all these commands in the VS code terminal in the same folder. 

Create a fresh environment: python -m venv .venv

Activate it: .\.venv\Scripts\activate

Install the tools: pip install -r requirements.txt

Sync the Database: python manage.py migrate